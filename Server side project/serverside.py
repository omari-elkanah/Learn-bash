#barebones
def listen():
    #listen for new connected devices on dhcp server
    #if connection is made, collect ip and mac address
    #find method to store list of mac addresses
    return "listen"

def Inquire():
    #get listen variables
    #get target name
    #get mac address
    #get os version
    return "Inquire"

if Inquire.version != #win 11 version number
    #load storage area
    #update in background; give bool(1) for mac address; store
    #wait for update from client and check if complete
    if update == True:#complete
        #give bool (1)
    else:
        #give bool (0)
else:
    #give bool (1) because it is win 11
    